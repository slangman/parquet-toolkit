plugins {
    id 'java'
    id 'maven-publish'
}

group 'kz.hustle'
version '0.1.0-SNAPSHOT'

sourceCompatibility = 1.8

repositories {
    maven {
        url "https://repository.cloudera.com/artifactory/cloudera-repos/"
    }
    mavenCentral()
    mavenLocal()
}

dependencies {
    testImplementation group: 'junit', name: 'junit', version: '4.12'
    testImplementation group: 'org.mockito', name: 'mockito-all', version: '1.10.19'
    testImplementation (group: 'org.apache.hadoop', name: 'hadoop-minicluster', version: '3.3.1') {
        exclude group: 'com.google.guava', module: 'guava'
        //exclude group: 'org.slf4j', module: 'slf4j-log4j12'
    }
    implementation group: 'org.apache.hadoop', name: 'hadoop-auth', version: '3.3.1'

    implementation group: 'org.apache.hadoop', name: 'hadoop-common', version: '3.3.1'
    implementation group: 'org.apache.hadoop', name: 'hadoop-hdfs', version: '3.3.1'
    implementation group: 'org.apache.hadoop', name: 'hadoop-hdfs-client', version: '3.3.1'
    //implementation group: 'org.apache.hadoop', name: 'hadoop-client-api', version: '3.3.3'
    //implementation group: 'org.apache.hadoop', name: 'hadoop-client-runtime', version: '3.3.3'
    //implementation group: 'org.apache.logging.log4j', name: 'log4j-core', version: '2.17.2'
    implementation group: 'org.apache.avro', name: 'avro', version: '1.10.2'
    implementation group: 'org.apache.parquet', name: 'parquet-hadoop', version: '1.12.1'
    implementation group: 'org.apache.parquet', name: 'parquet-protobuf', version: '1.12.2'
    //implementation group: 'org.apache.parquet', name: 'parquet-hadoop', version: '1.10.99.7.2.10.0-148'
    implementation group: 'org.apache.parquet', name: 'parquet-avro', version: '1.12.2'
    implementation group: 'org.apache.commons', name: 'commons-csv', version: '1.8'
    implementation group: 'org.xerial', name: 'sqlite-jdbc', version: '3.34.0'
}

configurations {
    //runtime.exclude group: "org.slf4j", module: "slf4j-log4j12"
    //runtime.exclude group: "log4j", module: "log4j"
}

//For building production jar
jar {
    sourceSets {
        main {
            java {
                exclude 'kz/beeline/dmc/test/**'
            }
        }
    }
}

//Publish in nexus
apply plugin: 'maven-publish'

publishing {
    publications {
        maven(MavenPublication) {
            artifact("build/libs/parquet-utils-$version" + ".jar") {
                extension 'jar'
            }
            generatePomFileForMavenPublication
        }
    }
    repositories {
        maven {
            url ""
            credentials {
                username ""
                password ""
            }
        }
    }
}

//Build fat jar
/*jar {
    manifest {
        attributes(
                'Main-Class': 'kz.beeline.dmc.test.TreeMultithreadedParquetMergerTest'
        )
    }
    from {
        configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) }
    }

    tasks.withType(JavaCompile) {
        options.encoding = 'UTF-8'
    }
}*/
